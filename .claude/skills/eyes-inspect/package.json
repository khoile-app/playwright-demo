{
  "name": "eyes-inspect",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"require('fs').copyFileSync(require('path').join(process.cwd(),'..','..','..','sessions','dom-search.md'), require('path').join(process.cwd(),'dom-search.md'))\"",
    "pretest": "npm run build",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "dotenv": "^16.6.1",
    "sharp": "^0.34.5",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
